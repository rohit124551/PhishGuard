# PhishGuard Backend — Python Dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Environment variable loading
python-dotenv==1.0.1

# WHOIS domain lookup (domain age check)
python-whois==0.9.4

# HTTP client (Safe Browsing API call)
requests==2.32.3

# TLD / domain extraction
tldextract==5.1.3

# Data validation (bundled with FastAPI but pinned for reproducibility)
pydantic==2.10.3

# Optional: faster JSON (used by uvicorn[standard])
# ujson is pulled in transitively — no explicit pin needed
